# Reference archive (old project)

Snapshots from the previous `tech-site` project — kept for context, **not**
live documentation. The current docs are `README.md`, `CHANGELOG.md`, and
`docs/` at the repo root.

- `Guides/` — old setup walkthroughs (run, database, APIs, deployment) and
  SQL history (`sql/history/schema-v4.0.sql`, seeds). Superseded by
  `supabase/schema.sql`, but useful for the retailer-affiliate key names
  and migration history.
- `design-research/3.0-editorial-research.md` — editorial research notes.
- `README-legacy.md` / `CHANGELOG-legacy.md` — old project's overview and
  version history (v4.0 era).
- `prompt.md` — the original project spec prompt.
